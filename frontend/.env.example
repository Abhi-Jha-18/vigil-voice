# Leave VITE_API_BASE_URL empty to use same-origin requests (recommended).
# In dev, Vite proxies /api -> http://127.0.0.1:8000 (see vite.config.js).
# VITE_API_BASE_URL=

# Optional: override the dev proxy target for the FastAPI backend.
# VITE_PROXY_TARGET=http://127.0.0.1:8000
