# Models Checkpoints

Place your trained model weights here.
The backend integrates:
* Phase 2 (CNN): Expects `models/cnn_weight.pth`
* Phase 3 (CNN-LSTM): Expects `models/cnn_lstm_weight.pth`
* Phase 4 (Wav2Vec): Expects `models/wav2vec_weight.pth`
